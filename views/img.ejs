<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Upload Profile Image</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white flex items-center justify-center min-h-screen">
  <div class="bg-gray-800 p-6 rounded-xl shadow-lg w-96">
    <h2 class="text-2xl font-semibold mb-4 text-center">Upload Profile Picture</h2>

    <form action="/img/<%= user._id %>" method="POST" enctype="multipart/form-data" class="flex flex-col gap-4">
      <input type="file" name="profilePic" accept="image/*" required 
        class="file:mr-4 file:py-2 file:px-4 file:rounded-full 
               file:border-0 file:text-sm file:font-semibold 
               file:bg-violet-50 file:text-violet-700 
               hover:file:bg-violet-100 bg-gray-700 p-2 rounded" />

      <button type="submit"
        class="bg-violet-600 hover:bg-violet-700 py-2 px-4 rounded text-white font-semibold">
        Upload
      </button>
    </form>
  </div>
</body>
</html>
